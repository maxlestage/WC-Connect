{
  "name": "wc-connect",
  "version": "1.0.0",
  "private": true,
  "description": "Dépôt WC Connect : application iOS/watchOS et site de présentation React + TypeScript.",
  "workspaces": [
    "website"
  ],
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "npm run dev --workspace website",
    "build": "npm run build --workspace website",
    "typecheck": "npm run typecheck --workspace website",
    "start": "node website/server-dist/server.js",
    "heroku-postbuild": "npm run build --workspace website"
  }
}
